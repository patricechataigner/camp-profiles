# Camp profiles — public report

A self-contained DIVE Profile report (single static page). No dataset, no server.

To update: in DIVE → Report → your Profile → Preview → **Export for web**, then
replace `index.html` here with the downloaded file and commit.
